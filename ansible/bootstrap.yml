---
- name: 🐣 Birth Stage - Bootstrap VPS
  hosts: vps
  become: "{{ ansible_user != 'root' }}"
  gather_facts: false
  tasks:
    - name: 🛠️ Ensure 'ops' user is configured
      ansible.builtin.import_role:
        name: ops_user
