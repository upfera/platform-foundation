---
k3s_cluster:
  children:
    server:
      hosts:
        "{{ lookup('env', 'K3S_SERVER_HOSTS') | split(',') | list | to_yaml | from_yaml }}":
    agent:
      hosts:
        "{{ lookup('env', 'K3S_AGENT_HOSTS') | split(',') | list | to_yaml | from_yaml }}":
