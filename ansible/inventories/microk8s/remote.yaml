# This is a sample inventory for a remote MicroK8s cluster.
# Real values should be provided by an external infrastructure source.
microk8s_cluster:
  hosts:
    10.0.0.5:
      ansible_host: 1.2.3.5
