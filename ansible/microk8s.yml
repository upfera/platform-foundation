---
- name: 🧩 MicroK8s Cluster Setup
  hosts: microk8s_cluster, local
  become: true
  tasks:
    - name: 📦 Install MicroK8s
      ansible.builtin.import_role:
        name: microk8s
      when: k8s_distro | default('microk8s') == 'microk8s'
